name: build & test

on: [push]

jobs:
  build-docker-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image: ghcr.io/${{ github.repository }}/tiliqua-builder:${{ steps.sanitize.outputs.tag }}
    steps:
      - uses: actions/checkout@v7
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Sanitize branch name for Docker tag
        id: sanitize
        run: |
          TAG=$(echo "${{ github.ref_name }}" | sed 's/\//-/g')
          echo "tag=$TAG" >> $GITHUB_OUTPUT
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: gateware/scripts
          file: gateware/scripts/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository }}/tiliqua-builder:${{ steps.sanitize.outputs.tag }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/tiliqua-builder:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository }}/tiliqua-builder:buildcache,mode=max

  minimal-bitstream:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7
      - name: Setup PDM
        uses: pdm-project/setup-pdm@v4
        with:
          python-version: '3.13'
      - run: |
          pdm install
          pdm dsp build
        working-directory: gateware
        env:
          PYTHONIOENCODING: utf-8

  unit-tests-amaranth:
    needs: build-docker-image
    runs-on: ubuntu-latest
    container:
      image: ${{ needs.build-docker-image.outputs.image }}
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-container
      - run: pdm test
        working-directory: gateware

  unit-tests-rust:
    needs: build-docker-image
    runs-on: ubuntu-latest
    container:
      image: ${{ needs.build-docker-image.outputs.image }}
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-container
      - run: | # fake generated SoC constants from lib
          touch src/generated_constants.rs
          RUST_LOG=debug cargo test
        working-directory: gateware/src/rs/lib
      - run: |
          RUST_LOG=debug cargo test_linux
        working-directory: gateware/src/rs/hal
      - run: |
          RUST_LOG=debug cargo test
        working-directory: gateware/src/rs/opts

  build-bitstreams-soc:
    needs: build-docker-image
    runs-on: ubuntu-latest
    container:
      image: ${{ needs.build-docker-image.outputs.image }}
    strategy:
      matrix:
        hw_rev: ["r2", "r3", "r4", "r5"]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-container
      - name: Build all SoC bitstreams (${{ matrix.hw_rev }})
        run: |
          # Use yosys/nextpnr from oss-cad-suite instead of yowasp-yosys for speed
          git update-index --skip-worktree .env.toolchain
          rm .env.toolchain
          ./scripts/build_bitstreams_soc.sh --hw=${{ matrix.hw_rev }}
        working-directory: gateware
      - name: Generate build summary
        if: always()
        run: |
          echo "## SoC Bitstreams Build Summary (${{ matrix.hw_rev }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          for timfile in build/*/*.tim; do
            if [ -f "$timfile" ]; then
              design=$(basename $(dirname "$timfile"))
              echo "### $design" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY

              # Extract device utilization
              sed -n '/Info: Device utilisation:/,/^$/p' "$timfile" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY

              # Extract timing info
              grep -E "Info: Max frequency for clock|Warning: Max frequency for clock" "$timfile" >> $GITHUB_STEP_SUMMARY || true

              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi
          done
        working-directory: gateware
      - uses: actions/upload-artifact@v7
        with:
          name: bitstreams-soc-${{ matrix.hw_rev }}.zip
          path: gateware/build/*/*-*-*.tar.gz
          retention-days: 1

  build-bitstreams-no-soc:
    needs: build-docker-image
    runs-on: ubuntu-latest
    container:
      image: ${{ needs.build-docker-image.outputs.image }}
    strategy:
      matrix:
        hw_rev: ["r2", "r3", "r4", "r5"]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-container
      - name: Build all non-SoC bitstreams (${{ matrix.hw_rev }})
        run: |
          # Use yosys/nextpnr from oss-cad-suite instead of yowasp-yosys for speed
          git update-index --skip-worktree .env.toolchain
          rm .env.toolchain
          ./scripts/build_bitstreams_no_soc.sh --hw=${{ matrix.hw_rev }}
        working-directory: gateware
      - name: Generate build summary
        if: always()
        run: |
          echo "## Non-SoC Bitstreams Build Summary (${{ matrix.hw_rev }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          for timfile in build/*/*.tim; do
            if [ -f "$timfile" ]; then
              design=$(basename $(dirname "$timfile"))
              echo "### $design" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY

              # Extract device utilization
              sed -n '/Info: Device utilisation:/,/^$/p' "$timfile" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY

              # Extract timing info
              grep -E "Info: Max frequency for clock|Warning: Max frequency for clock" "$timfile" >> $GITHUB_STEP_SUMMARY || true

              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
            fi
          done
        working-directory: gateware
      - uses: actions/upload-artifact@v7
        with:
          name: bitstreams-no-soc-${{ matrix.hw_rev }}.zip
          path: gateware/build/*/*-*-*.tar.gz
          retention-days: 1

  sim-bitstream:
    needs: build-docker-image
    runs-on: ubuntu-latest
    container:
      image: ${{ needs.build-docker-image.outputs.image }}
    strategy:
      matrix:
        config:
          - name: dsp-diffuser-sim
            cmd: dsp sim --dsp-core psram_diffuser
          - name: vectorscope-sim
            cmd: vectorscope_no_soc sim --fs-192khz
        hw_rev: ["r2", "r3", "r4", "r5"]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-container
      - name: Build ${{ matrix.config.cmd }} --hw ${{ matrix.hw_rev }}
        run: pdm ${{ matrix.config.cmd }} --hw ${{ matrix.hw_rev }}
        working-directory: gateware

  package-artifacts:
    needs: [build-bitstreams-soc, build-bitstreams-no-soc]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: "*"
          path: .
          merge-multiple: true
      - uses: actions/upload-artifact@v7
        with:
          name: bitstreams
          path: "*"
